// ([[Node]], Int) ->  Boolean
export const lessThanNStops = n => route => false;

// ([[Node]], Int) ->  Boolean
export const exactlyNStops = n => route => false;

// ([[Node]], Int) ->  Boolean
export const distanceLessThanN = n => route => false;
